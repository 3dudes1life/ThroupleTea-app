{
  "name": "throupletea-app",
  "version": "1.0.0",
  "private": true,
  "description": "A Little Throuple Tea iOS app built with Capacitor.",
  "scripts": {
    "sync:ios": "npx cap sync ios",
    "open:ios": "npx cap open ios",
    "update:ios": "npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
